---
import Layout from '../layouts/Layout.astro';
import SmartModalAlpine from '../components/SmartModalAlpine.astro';
---

<Layout 
  title="Test Alpine Smart Modal" 
  description="Testing the new Alpine.js Smart Modal system"
>
  <div class="min-h-screen bg-gray-50 py-12">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="bg-white rounded-lg shadow-lg p-8">
        <h1 class="text-3xl font-bold text-gray-900 mb-8 text-center">
          Alpine.js Smart Modal Test
        </h1>
        
        <div class="space-y-6">
          <div class="text-center">
            <h2 class="text-xl font-semibold mb-4">Test the New Modal System</h2>
            <p class="text-gray-600 mb-8">
              This uses Alpine.js for much better performance and reliability.
            </p>
          </div>
          
          <!-- Button Grid -->
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            
            <!-- Primary CTA Button -->
            <button 
              onclick="window.openSmartModal()"
              class="bg-red-500 hover:bg-red-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors"
            >
              Open Smart Modal
            </button>
            
            <!-- Alternative trigger -->
            <button 
              onclick="window.smartModalInstance?.open()"
              class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors"
            >
              Direct Instance Call
            </button>
            
            <!-- Legacy compatibility -->
            <button 
              onclick="window.openOffersModal()"
              class="bg-green-500 hover:bg-green-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors"
            >
              Legacy openOffersModal
            </button>
            
            <!-- Test logging -->
            <button 
              onclick="console.log('Window functions:', Object.keys(window).filter(k => k.includes('Modal') || k.includes('smart')))"
              class="bg-purple-500 hover:bg-purple-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors"
            >
              Test Console
            </button>
            
            <!-- Test Alpine -->
            <button 
              x-data
              @click="$dispatch('test-alpine')"
              class="bg-yellow-500 hover:bg-yellow-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors"
            >
              Test Alpine Event
            </button>
            
            <!-- Manual test -->
            <button 
              class="bg-gray-500 hover:bg-gray-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors"
              onclick="
                try {
                  console.log('Testing manual open...');
                  if (window.smartModalInstance) {
                    window.smartModalInstance.open();
                  } else {
                    alert('smartModalInstance not found');
                  }
                } catch (e) {
                  console.error('Error:', e);
                  alert('Error: ' + e.message);
                }
              "
            >
              Manual Test
            </button>
          </div>
          
          <div class="mt-12 bg-gray-100 rounded-lg p-6">
            <h3 class="text-lg font-semibold mb-4">What's New:</h3>
            <ul class="space-y-2 text-sm text-gray-700">
              <li>• <strong>Alpine.js</strong> - Lightweight, reliable JavaScript framework</li>
              <li>• <strong>Declarative</strong> - No complex event binding issues</li>
              <li>• <strong>Better Performance</strong> - Only 15KB, loads faster</li>
              <li>• <strong>Built for Astro</strong> - Perfect integration with SSG</li>
              <li>• <strong>Reactive</strong> - State changes automatically update UI</li>
            </ul>
          </div>
          
          <div class="mt-8 bg-blue-50 rounded-lg p-6">
            <h3 class="text-lg font-semibold mb-4">Expected Behavior:</h3>
            <ol class="space-y-2 text-sm text-gray-700 list-decimal list-inside">
              <li>Click any button above to open the modal instantly</li>
              <li>Modal should have smooth animations</li>
              <li>Select a situation to see appropriate next step</li>
              <li>Fill out the booking form to test validation</li>
              <li>Close with X button, ESC key, or clicking outside</li>
            </ol>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Include the Smart Modal -->
  <SmartModalAlpine />
</Layout>

<script>
  // Additional debugging
  document.addEventListener('DOMContentLoaded', () => {
    console.log('🧪 Test page loaded');
    console.log('Available functions:', Object.keys(window).filter(k => k.includes('Modal') || k.includes('smart')));
    
    // Test Alpine after it loads
    setTimeout(() => {
      console.log('Alpine.js available:', typeof Alpine !== 'undefined');
      if (window.smartModalInstance) {
        console.log('✅ smartModalInstance found');
      } else {
        console.log('❌ smartModalInstance not found');
      }
    }, 1000);
  });
  
  // Listen for Alpine events
  document.addEventListener('test-alpine', () => {
    console.log('🎉 Alpine.js is working!');
    alert('Alpine.js is working correctly!');
  });
</script>