---
import Layout from '../../../layouts/Layout.astro';

const deductibleLevels = [
  {
    type: "Adults",
    base: 300,
    options: [300, 500, 1000, 1500, 2000, 2500],
    description: "Higher deductibles lead to lower monthly premiums"
  },
  {
    type: "Children",
    base: 0,
    options: [0, 100, 200, 300, 400, 500, 600],
    description: "Children under 18 are not required to pay annual deductible"
  }
];

const costComponents = [
  {
    name: "Annual Deductible",
    description: "The amount you must pay before insurance coverage begins",
    icon: `<svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2" />
    </svg>`
  },
  {
    name: "Retention Fee",
    description: "10% of costs after deductible (max CHF 700 for adults, CHF 350 for children)",
    icon: `<svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 8h6m-5 0a3 3 0 110 6H9l3 3m-3-6h6m6 1a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>`
  },
  {
    name: "Hospital Stay",
    description: "CHF 15 per day (except for children, maternity, and students)",
    icon: `<svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
    </svg>`
  }
];
---

<Layout
  title="Annual Deductible in Swiss Health Insurance | Expat Savvy"
  description="Understand how annual deductibles work in Swiss health insurance. Learn about deductible levels, retention fees, and ways to reduce your premiums."
>
  <!-- Hero Section -->
  <div class="bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
      <div class="text-center">
        <h1 class="text-4xl font-extrabold text-gray-900 sm:text-5xl md:text-6xl">
          Annual Deductible
        </h1>
        <p class="mt-6 max-w-2xl mx-auto text-xl text-gray-500">
          Annual deductible represents the amount you are required to pay towards your treatment costs on a yearly basis. When you reach this amount, the insurance company starts distributing payments.
        </p>
      </div>
    </div>
  </div>

  <!-- Cost Components -->
  <div class="bg-gray-50 py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-3xl font-bold text-gray-900 mb-12 text-center">Understanding Your Healthcare Costs</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {costComponents.map(component => (
          <div class="bg-white rounded-lg shadow-md p-6 transform transition-transform hover:scale-105">
            <div class="text-primary-500 mb-4">
              <Fragment set:html={component.icon} />
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2">{component.name}</h3>
            <p class="text-gray-600">{component.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- Deductible Levels -->
  <div class="bg-white py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-3xl font-bold text-gray-900 mb-12 text-center">Deductible Levels</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        {deductibleLevels.map(level => (
          <div class="bg-white rounded-lg shadow-md p-6 border border-gray-200">
            <h3 class="text-xl font-semibold text-gray-900 mb-4">{level.type}</h3>
            <p class="text-gray-600 mb-4">{level.description}</p>
            <div class="flex flex-wrap gap-2">
              {level.options.map(option => (
                <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-primary-100 text-primary-800">
                  CHF {option}
                </span>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>

  <!-- How Costs Are Calculated -->
  <div class="bg-gray-50 py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="prose prose-lg mx-auto">
        <h2 class="text-3xl font-bold text-gray-900 text-center mb-8">How Costs Are Calculated</h2>
        <div class="bg-white rounded-lg shadow-md p-8">
          <p class="mb-6">
            All Swiss citizens have to take out a basic insurance policy and pay monthly premiums to the insurance company of their choice. When you need to undergo any kind of medical treatment, you are required to cover a part of the costs, calculated based on:
          </p>
          <ul class="space-y-4">
            <li class="flex items-start">
              <svg class="h-6 w-6 text-primary-500 mt-1 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
              </svg>
              <span>Your annual deductible amount</span>
            </li>
            <li class="flex items-start">
              <svg class="h-6 w-6 text-primary-500 mt-1 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
              </svg>
              <span>The retention fee (10% of costs above deductible)</span>
            </li>
            <li class="flex items-start">
              <svg class="h-6 w-6 text-primary-500 mt-1 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
              </svg>
              <span>Hospital stay costs (if applicable)</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <!-- Reducing Premiums -->
  <div class="bg-white py-16">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-3xl mx-auto">
        <h2 class="text-3xl font-bold text-gray-900 text-center mb-8">Reducing Your Insurance Premium</h2>
        <div class="bg-primary-50 rounded-lg p-8">
          <p class="text-gray-800 mb-4">
            You can reduce your insurance premium by increasing your deductible. However, there are important considerations:
          </p>
          <ul class="space-y-4">
            <li class="flex items-start">
              <svg class="h-6 w-6 text-primary-500 mt-1 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <span class="text-gray-800">Changes to deductible can only be made at the beginning of a calendar year</span>
            </li>
            <li class="flex items-start">
              <svg class="h-6 w-6 text-primary-500 mt-1 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <span class="text-gray-800">Higher deductibles mean lower monthly premiums but higher out-of-pocket costs when you need care</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <!-- CTA Section -->
  <div class="bg-primary-600">
    <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h2 class="text-3xl font-extrabold text-white sm:text-4xl">
          Need help choosing the right deductible?
        </h2>
        <p class="mt-4 text-xl text-primary-100">
          Get personalized advice from our insurance experts
        </p>
        <div class="mt-8">
          <button
            onclick="showConsultationModal()"
            class="inline-flex items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-primary-600 bg-white hover:bg-primary-50 transition-colors duration-300"
          >
            Schedule Free Consultation
          </button>
        </div>
      </div>
    </div>
  </div>
</Layout>