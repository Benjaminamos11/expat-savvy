---
import { Image } from "astro:assets";

interface Props {
    name: string;
    title: string;
    image: string;
    credentials?: string;
    bio?: string;
    size?: "small" | "medium" | "large";
    className?: string;
}

const {
    name,
    title,
    image,
    credentials = "FINMA-Certified Advisor",
    bio,
    size = "medium",
    className = "",
} = Astro.props;

const cardClasses = {
    small: "p-3 max-w-[200px]",
    medium: "p-6 max-w-sm",
    large: "p-8 max-w-md",
};

const imageSizes = {
    small: "w-12 h-12",
    medium: "w-16 h-16",
    large: "w-24 h-24",
};

const nameSizes = {
    small: "text-sm",
    medium: "text-lg",
    large: "text-xl",
};
---
