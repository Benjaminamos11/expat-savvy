---
---

<section class="py-16 bg-gradient-to-br from-red-600 via-red-700 to-red-800 text-white relative overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-10">
    <div class="absolute inset-0" style="background-image: radial-gradient(circle at 2px 2px, white 1px, transparent 0); background-size: 40px 40px;"></div>
  </div>

  <div class="max-w-5xl mx-auto px-4 relative z-10">
    <div class="text-center mb-12">
      <div class="inline-block px-4 py-2 bg-white/20 rounded-full text-sm font-semibold mb-4">
        ⚠️ CRITICAL DEADLINE
      </div>
      <h2 class="text-4xl md:text-5xl font-bold mb-4">November 30, 2025</h2>
      <p class="text-xl text-red-100">Last day to switch insurance for 2026 savings</p>
    </div>

    <!-- Countdown Timer -->
    <div class="bg-white/10 backdrop-blur-lg rounded-2xl p-8 mb-12 border-2 border-white/20">
      <div class="grid grid-cols-4 gap-4 md:gap-8">
        <!-- Days -->
        <div class="text-center">
          <div class="countdown-number text-4xl md:text-6xl font-bold mb-2" id="countdown-days">42</div>
          <div class="text-sm md:text-base text-red-100">Days</div>
        </div>
        <!-- Hours -->
        <div class="text-center">
          <div class="countdown-number text-4xl md:text-6xl font-bold mb-2" id="countdown-hours">18</div>
          <div class="text-sm md:text-base text-red-100">Hours</div>
        </div>
        <!-- Minutes -->
        <div class="text-center">
          <div class="countdown-number text-4xl md:text-6xl font-bold mb-2" id="countdown-minutes">23</div>
          <div class="text-sm md:text-base text-red-100">Minutes</div>
        </div>
        <!-- Seconds -->
        <div class="text-center">
          <div class="countdown-number text-4xl md:text-6xl font-bold mb-2" id="countdown-seconds">45</div>
          <div class="text-sm md:text-base text-red-100">Seconds</div>
        </div>
      </div>
    </div>

    <!-- Consequences -->
    <div class="grid md:grid-cols-2 gap-6 mb-12">
      <!-- Miss Deadline -->
      <div class="bg-white/10 backdrop-blur-lg rounded-xl p-6 border-2 border-white/20">
        <h3 class="text-xl font-bold mb-4 flex items-center">
          <span class="text-3xl mr-3">❌</span>
          Miss the Deadline
        </h3>
        <ul class="space-y-3 text-red-100">
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Locked into current insurer for entire 2026</span>
          </li>
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Lose potential savings of CHF 1,800-3,600</span>
          </li>
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Premium increases take effect with no alternatives</span>
          </li>
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Cannot switch to better English support</span>
          </li>
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Next opportunity: November 2026</span>
          </li>
        </ul>
      </div>

      <!-- Act Now -->
      <div class="bg-white/20 backdrop-blur-lg rounded-xl p-6 border-2 border-white/40">
        <h3 class="text-xl font-bold mb-4 flex items-center">
          <span class="text-3xl mr-3">✅</span>
          Act Before Deadline
        </h3>
        <ul class="space-y-3">
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Save up to CHF 3,600 annually</span>
          </li>
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Switch to premium English support</span>
          </li>
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Access better digital tools & wellness programs</span>
          </li>
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Coverage guaranteed regardless of health</span>
          </li>
          <li class="flex items-start">
            <span class="mr-2">•</span>
            <span>Free expert consultation & paperwork handling</span>
          </li>
        </ul>
      </div>
    </div>

    <!-- Special Cases -->
    <div class="bg-yellow-400 text-gray-900 rounded-xl p-6 mb-8">
      <h3 class="text-xl font-bold mb-4">⚡ Important for Expats:</h3>
      <div class="grid md:grid-cols-2 gap-4 text-sm">
        <div>
          <strong>New arrivals:</strong> You have 3 months from registration (not bound by November 30)
        </div>
        <div>
          <strong>Current residents:</strong> Must send registered cancellation BY November 30
        </div>
        <div>
          <strong>Coverage continuity:</strong> Swiss law requires no gaps in insurance
        </div>
        <div>
          <strong>Work permit holders:</strong> Insurance required regardless of employer
        </div>
      </div>
    </div>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center">
      <button 
        onclick="window.openOffersModal && window.openOffersModal('deadline-urgent')"
        class="px-8 py-4 bg-white text-red-600 hover:bg-red-50 rounded-xl font-bold text-lg shadow-xl hover:shadow-2xl transition-all"
      >
        🚨 Get Urgent Deadline Help
      </button>
      <button 
        onclick="window.openOffersModal && window.openOffersModal('deadline-calculate')"
        class="px-8 py-4 bg-white/20 hover:bg-white/30 backdrop-blur border-2 border-white/40 rounded-xl font-bold text-lg transition-all"
      >
        Calculate My Savings Now
      </button>
    </div>

    <!-- Urgency Message -->
    <div class="text-center mt-8">
      <p class="text-lg text-red-100">
        Processing applications takes 1-2 weeks. <strong class="text-white">Don't wait until the last minute!</strong>
      </p>
    </div>
  </div>
</section>

<script>
  // Countdown Timer
  function updateCountdown() {
    const deadline = new Date('2025-11-30T23:59:59').getTime();
    const now = new Date().getTime();
    const diff = deadline - now;

    if (diff > 0) {
      const days = Math.floor(diff / (1000 * 60 * 60 * 24));
      const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((diff % (1000 * 60)) / 1000);

      const daysEl = document.getElementById('countdown-days');
      const hoursEl = document.getElementById('countdown-hours');
      const minutesEl = document.getElementById('countdown-minutes');
      const secondsEl = document.getElementById('countdown-seconds');

      if (daysEl) daysEl.textContent = String(days);
      if (hoursEl) hoursEl.textContent = hours.toString().padStart(2, '0');
      if (minutesEl) minutesEl.textContent = minutes.toString().padStart(2, '0');
      if (secondsEl) secondsEl.textContent = seconds.toString().padStart(2, '0');
    } else {
      // Deadline passed
      const daysEl = document.getElementById('countdown-days');
      const hoursEl = document.getElementById('countdown-hours');
      const minutesEl = document.getElementById('countdown-minutes');
      const secondsEl = document.getElementById('countdown-seconds');

      if (daysEl) daysEl.textContent = '0';
      if (hoursEl) hoursEl.textContent = '00';
      if (minutesEl) minutesEl.textContent = '00';
      if (secondsEl) secondsEl.textContent = '00';
    }
  }

  // Update countdown every second
  updateCountdown();
  setInterval(updateCountdown, 1000);

  // Pulse animation for numbers
  setInterval(() => {
    document.querySelectorAll('.countdown-number').forEach(el => {
      const element = el as HTMLElement;
      element.style.transform = 'scale(1.05)';
      setTimeout(() => {
        element.style.transform = 'scale(1)';
      }, 200);
    });
  }, 1000);
</script>

<style>
  .countdown-number {
    transition: transform 0.2s ease;
  }
</style>

