---
// Sticky Navigation Component
---

<div id="sticky-nav" class="hidden lg:block fixed top-0 left-0 right-0 bg-white shadow-md z-50" style="display: none;">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <nav class="flex items-center justify-center py-4">
      <div class="flex space-x-8">
        <a href="#key-differences" class="text-sm font-medium text-gray-600 hover:text-red-600 transition-colors">Key Differences</a>
        <a href="#products" class="text-sm font-medium text-gray-600 hover:text-red-600 transition-colors">Products</a>
        <a href="#who-chooses-what" class="text-sm font-medium text-gray-600 hover:text-red-600 transition-colors">Who Chooses What</a>
        <a href="#why-us" class="text-sm font-medium text-gray-600 hover:text-red-600 transition-colors">Why Us</a>
        <a href="#how-it-works" class="text-sm font-medium text-gray-600 hover:text-red-600 transition-colors">How It Works</a>
        <a href="#faqs" class="text-sm font-medium text-gray-600 hover:text-red-600 transition-colors">FAQs</a>
        <a href="#related-links" class="text-sm font-medium text-gray-600 hover:text-red-600 transition-colors">Resources</a>
      </div>
    </nav>
  </div>
</div>

<!-- Mobile Sticky CTA -->
<div class="lg:hidden fixed bottom-0 left-0 right-0 bg-white border-t border-gray-200 p-4 z-50">
  <button 
    onclick="window.openHealthModal('consultation')"
    class="w-full inline-flex items-center justify-center px-6 py-3 bg-gradient-to-r from-red-600 to-red-700 hover:from-red-700 hover:to-red-800 text-white font-semibold rounded-lg shadow-lg transition-all"
  >
    Compare Swica & Helsana →
  </button>
</div>

<script>
  // Show sticky nav after hero section
  window.addEventListener('scroll', function() {
    const hero = document.getElementById('hero');
    const stickyNav = document.getElementById('sticky-nav');
    
    if (hero && stickyNav) {
      const heroBottom = hero.offsetTop + hero.offsetHeight;
      
      if (window.scrollY > heroBottom) {
        stickyNav.style.display = 'block';
      } else {
        stickyNav.style.display = 'none';
      }
    }
  });

  // Smooth scroll for anchor links
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e: Event) {
      e.preventDefault();
      const href = (e.target as HTMLAnchorElement).getAttribute('href');
      const target = href ? document.querySelector(href) : null;
      if (target) {
        target.scrollIntoView({
          behavior: 'smooth',
          block: 'start'
        });
      }
    });
  });
</script>
