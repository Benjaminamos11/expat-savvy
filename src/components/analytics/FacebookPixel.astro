---
/**
 * Facebook/Meta Pixel Component - CONSENT-FIRST LOADING
 * 
 * Pixel ID: 1393006485701609
 * 
 * OPTIMIZATION: Only loads fbevents.js AFTER user accepts cookies
 * - Zero impact on PageSpeed until consent given
 * - Stub functions available immediately for tracking calls
 * - Full script loads only when needed
 */
---

<!-- Meta Pixel - Loads only on consent -->
<script is:inline>
(function() {
  'use strict';
  
  // Hardcoded pixel ID to avoid variable interpolation issues
  var PIXEL_ID = '1393006485701609';
  
  var pixelLoaded = false;
  var queuedEvents = [];
  
  // Stub fbq function to queue events before pixel loads
  window.fbq = window.fbq || function() {
    if (pixelLoaded && window._fbq) {
      window._fbq.apply(null, arguments);
    } else {
      queuedEvents.push(arguments);
    }
  };
  
  // Function to load the actual pixel script
  window.loadMetaPixel = function() {
    if (pixelLoaded) return;
    pixelLoaded = true;
    
    // Load the Facebook Pixel script
    !function(f,b,e,v,n,t,s) {
      n = f.fbq = function() {
        n.callMethod ? n.callMethod.apply(n, arguments) : n.queue.push(arguments)
      };
      if(!f._fbq) f._fbq = n;
      n.push = n;
      n.loaded = !0;
      n.version = '2.0';
      n.queue = [];
      t = b.createElement(e);
      t.async = !0;
      t.src = v;
      s = b.getElementsByTagName(e)[0];
      s.parentNode.insertBefore(t, s);
    }(window, document, 'script', 'https://connect.facebook.net/en_US/fbevents.js');
    
    // Initialize with consent granted
    fbq('consent', 'grant');
    fbq('init', PIXEL_ID);
    fbq('track', 'PageView');
    
    // Process any queued events
    for (var i = 0; i < queuedEvents.length; i++) {
      fbq.apply(null, queuedEvents[i]);
    }
    queuedEvents = [];
    
    console.log('[MetaPixel] Script loaded with ID:', PIXEL_ID);
  };
  
  // Listen for consent updates
  window.addEventListener('consentUpdate', function(e) {
    if (e.detail && e.detail.granted) {
      window.loadMetaPixel();
    }
  });

  // Helper functions (queue events if pixel not loaded)
  window.trackMetaEvent = function(eventName, params) {
    if (pixelLoaded) {
      fbq('track', eventName, params || {});
    } else {
      queuedEvents.push(['track', eventName, params || {}]);
    }
  };

  window.trackMetaCustomEvent = function(eventName, params) {
    if (pixelLoaded) {
      fbq('trackCustom', eventName, params || {});
    } else {
      queuedEvents.push(['trackCustom', eventName, params || {}]);
    }
  };
  
  // Check for existing consent on page load
  try {
    var consent = localStorage.getItem('cookie_consent');
    if (consent === 'accepted') {
      // User previously accepted - load the pixel
      window.loadMetaPixel();
    }
  } catch (e) {}
})();
</script>
